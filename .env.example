# 站点绝对地址，影响 canonical / sitemap / RSS 里的 URL。
# 国内节点和海外节点部署同一份代码时，各自设置成对应域名。
NEXT_PUBLIC_SITE_URL=https://sourcecode.school

# 接单表单的转发地址（企业微信机器人 / 飞书机器人 / 自建接口都行）。
# 不设置时表单仍可提交，内容只会打到服务端日志里。
INQUIRY_WEBHOOK_URL=
